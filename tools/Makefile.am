if WITH_PYTHON
PYTHON_DIR = Python
else
PYTHON_DIR =
endif

if WITH_JAVA
JAVA_DIR = Java
else
JAVA_DIR =
endif

if WITH_PERL5
PERL5_DIR = Perl5
else
PERL5_DIR =
endif

if WITH_TCL
TCL_DIR = Tcl
else
TCL_DIR =
endif

SUBDIRS = .

EXTRA_DIST = Python Java Perl5 Tcl